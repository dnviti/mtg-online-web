apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mtgate.serviceAccountName" . }}
  labels:
    {{- include "mtgate.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
